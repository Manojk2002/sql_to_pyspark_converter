# SQL → PySpark SQL AI Framework — Python dependencies
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
flask>=3.0.0
werkzeug>=3.0.0

# ── SQL parsing & transpilation ───────────────────────────────────────────────
sqlglot>=23.0.0

# ── AI / LLM integration — choose one or more providers ──────────────────────
# Provider 1: OpenAI (GPT-4.1-mini — low-cost, paid)
openai>=1.30.0
python-dotenv>=1.0.0

# Provider 2: HuggingFace Inference API (free tier)
# huggingface-hub>=0.23.0

# Provider 3: Google Gemini (free 1M tokens/month)
# google-generativeai>=0.7.0

# Provider 4: Ollama (local, completely free — no package needed, uses built-in urllib)

# ── ADF Data Export (SQL DB → Excel/CSV/Parquet) ──────────────────────────────
sqlalchemy>=2.0.0
pandas>=2.1.0
openpyxl>=3.1.0
pyarrow>=14.0.0
# ── Database drivers (install the ones you need) ──────────────────────────────
psycopg2-binary>=2.9.0   # PostgreSQL
pymysql>=1.1.0           # MySQL / MariaDB
pyodbc>=5.1.0            # SQL Server / Azure SQL (requires ODBC Driver 17)
# cx-Oracle>=8.3.0       # Oracle (also requires Oracle Instant Client)
# redshift-connector>=2.0.0   # Amazon Redshift
# snowflake-sqlalchemy>=1.5.0 # Snowflake
# pymysql>=1.1.0           # uncomment for MySQL
# pyodbc>=5.1.0            # uncomment for SQL Server

# ── Document generation ───────────────────────────────────────────────────────
python-docx>=1.1.0
matplotlib>=3.8.0
numpy>=1.26.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Optional: local Spark validation (large install ~500MB) ───────────────────
# pyspark>=3.5.0
